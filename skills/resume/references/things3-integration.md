# Things3 Integration

All Things3 MCP tool patterns, task creation, querying, and fallback behavior are owned by the **`things3` tool skill**.

For resume specifically: use `mcp__things3__view-todos` with `list_name: "Today"` to get today's tasks.
