# Patterns

Reusable patterns and copy-paste references.

One file per pattern. Use the schema at `../_schemas/pattern.schema.md`.
Stores working command sequences, AppleScript snippets, API call patterns, etc.
