# Environments

Environment references for servers, VMs, and machines you manage.

One file per environment. Use the schema at `../_schemas/environment.schema.md`.
Captures connection info, services, key paths, and current state.
