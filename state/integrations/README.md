# Integrations

Integration references for external services (email, calendar, task managers, APIs).

One file per integration. Use the schema at `../_schemas/integration.schema.md`.
Documents connection details, key operations, and gotchas — never credentials.
